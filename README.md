# Tenzo-X-DSI
Direct syscall DLL injector. Bypasses API hooks via Halo's Gate SSN recovery, RWX allocation, and manual PE mapping. Includes ETW patching, PEB debug flag clearing, and header wiping. Console UI with CLI support.
